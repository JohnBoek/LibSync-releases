<?xml version="1.0" encoding="UTF-8"?>
<!--
  AutoUpdater.NET feed for BoekSolutions.LibSync.

  This file is hand-edited as part of the manual publish process — never generated or pushed
  automatically. <version> must always match the currently published installer's app version
  (LibSync.UI.csproj <Version> at build time), and <url> must point at that exact installer asset
  on this repo's Releases page.

  Current state: v1.2.2 is LIVE (2026-09-23). GitHub latest metadata confirms the
  LibSync-Setup.exe asset. Unauthenticated HEAD returned 200 and Content-Length
  55321824; the full public download also matched the tested installer's byte size and
  SHA-256 5675db85140f27d1e7c4563b41bd7ca2b6bf7e6412fabb7e227ccd2409b112c7.
  These checks completed before updating the version below.

  The installer's output filename is a FIXED "LibSync-Setup.exe" (no version suffix, see
  protect/installer/LibSync.iss) from day one, so both this file and the website's download button
  can always use GitHub's version-independent "latest" permalink instead of a per-release URL:
      https://github.com/JohnBoek/LibSync-releases/releases/latest/download/LibSync-Setup.exe
  Note: GitHub release tags have had inconsistent casing on other Boek Solutions products in the
  past (e.g. lowercase "v1.1.0" vs capital "V1.1.1") — always verify the actual tag via the GitHub
  API before wiring a per-version URL, don't assume a casing convention holds.
-->
<item>
    <version>1.2.2</version>
    <url>https://github.com/JohnBoek/LibSync-releases/releases/latest/download/LibSync-Setup.exe</url>
    <changelog>https://github.com/JohnBoek/LibSync-releases/blob/main/CHANGELOG.md</changelog>
    <mandatory>false</mandatory>
</item>
